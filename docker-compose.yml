version: "3.9"
services:
  web:
    build: .
    command: gunicorn config.wsgi:application --bind 0.0.0.0:8000
    ports:
      - "8000:8000"
    env_file:
      - .env
  bot:
    build: .
    command: python -m bot.runner
    env_file:
      - .env
